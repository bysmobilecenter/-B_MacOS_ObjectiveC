# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'macOS-oc' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!
   pod 'AppCenter'
   pod 'AppCenter/Push'
  # Pods for macOS-oc

  target 'macOS-ocTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'macOS-ocUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
